#include<iostream>
#include<string.h>
#include<cstdio>
using namespace std;
int main(){
    int N;
    cin >> N;
    char a[100],b[100];
    double temp;
    for(int i = 0; i < N; i++){
        cin >> str[i];
        sscanf(str, "%.2f", &temp);
        sprintf(str,"%.2lf",temp);
        if
    }
    return 0;
}